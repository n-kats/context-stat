# 計画

## 当面の計画

- コマンドごとの実例と失敗時の診断例を追加する。
- JSONの公開スキーマとtable/treeの表示例を手順へ追加する。
- 実装の責務変更に合わせて設計書の現行実装欄を更新する。
- 追加の画像方式やオンラインバックエンドは、公式仕様と送信条件を確認してから検討する。

採用済みの方針は`../設計判断/`へ移し、完了した項目はここに残さず`../現状/`へ反映します。
