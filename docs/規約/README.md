# 規約

- 本リポジトリの文書と対話は日本語で書く。
- 文書は短く保ち、利用方法は`手順`、判断基準は`仕様`、決定理由は`設計判断`へ分ける。
- 外部情報は`外部参照`へ記録し、参照先だけで現行挙動を説明しない。
- 実装やディレクトリを変更したときは、`目次.md`と関連文書を更新する。
- コマンド出力例は実行確認した内容を使い、抜粋の場合は抜粋であることが分かるようにする。
- 検証コマンドと確認結果は作業ログへ残す。
