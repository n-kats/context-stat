# 作業ログ: 比較機能取り下げ

## TODO
- [x] 現行仕様に比較機能があるか確認する
- [x] 比較関連の実装・テスト・ドキュメント追加を取り下げる
- [x] 既存のstat/table/JSON/tree実装を維持する
- [x] pytest、ruff、format、lockを確認する

## 決定事項
- 現行の`docs/spec/context-stat機能仕様.md`に比較コマンドはないため、比較機能は成果物に含めない。
- 比較関連の作業ログは履歴として残し、実装・仕様・READMEからは除去する。

## 未決
- なし。

## 確認手順
- `uv run pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv lock --check`
- `compare`がCLIヘルプ・仕様・README・ソースに残っていないことを確認する。

## 変更した主要パス
- `src/context_stat/cli.py`
- `src/context_stat/output.py`
- `src/context_stat/domain/comparison.py`
- `tests/unit/test_cli.py`
- `tests/unit/test_comparison.py`
- `docs/spec/context-stat機能仕様.md`
- `README.md`
- `_worklist/20260808-021617比較機能取り下げ.md`

## 実施結果

- 比較CLI、比較ドメイン、比較出力、比較テストを削除した。
- 現行仕様、README、設計、ディレクトリ一覧から比較追加分を削除した。
- 比較関連の過去ログは取り下げの注記を付けて保持した。
- `uv run pytest -q`は48件通過した。
- `uv run ruff check .`、`uv run ruff format --check .`、`uv lock --check`は通過した。
- `UV_TOOL_DIR=/tmp/context-stat-tools UV_TOOL_BIN_DIR=/tmp/context-stat-bin uv tool install . --force --no-cache`でインストールし、インストール後のヘルプに`compare`がないことを確認した。
- 比較以外の現行仕様にある`stat`、`jinja`、`git diff`、`mcp list`、`mcp request`、`completions`は維持した。
