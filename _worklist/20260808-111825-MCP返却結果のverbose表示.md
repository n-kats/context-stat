# MCP返却結果のverbose表示

## 対象パス

- `src/context_stat/cli.py`
- `src/context_stat/output.py`
- `tests/unit/test_cli.py`
- `tests/unit/test_output.py`
- `README.md`
- `docs/`
- `_worklist/`

## TODO

- [x] `-v`/`--verbose`を共通オプションとして追加する。
- [x] MCP返却結果本文をverbose時だけ表示する。
- [x] verboseなしのJSONから返却本文を除外する。
- [x] テストとドキュメントを更新する。
- [x] 検証する。

## 決定事項

- 通常の`mcp request`は計測表を表示し、返却結果本文は表示しない。
- `-v`/`--verbose`を指定した場合だけ、計測表の後に返却結果本文を表示する。
- MCP結果の状態、返却contentの種類、画像寸法などの計測事実はverbose指定に関係なく保持する。

## 未決

- なし。

## 確認手順

- `uv run pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv lock --check`
- `git diff --check`

## 結果

- ルート共通オプションに`-v`/`--verbose`を追加した。
- 通常の`mcp request`は計測表と結果状態を表示し、返却結果本文は表示しないようにした。
- `-v`/`--verbose`指定時だけ、table・treeの本文とJSONの`facts.result.value`を表示するようにした。
- MCP結果の状態、返却contentの要約、画像寸法、診断はverbose指定に関係なく保持する。
- README、仕様、手順、設計、設計判断を更新した。
- `uv run pytest -q`: 82 passed
- `uv run --extra tokenizers pytest -q`: 82 passed
- `uv run ruff check .`: 成功
- `uv run ruff format --check .`: 成功
- `uv lock --check`: 成功
- `git diff --check`: 成功
