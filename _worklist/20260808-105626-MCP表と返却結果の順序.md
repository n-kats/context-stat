# MCP表と返却結果の順序

## 対象パス

- `src/context_stat/output.py`
- `tests/unit/test_output.py`
- `docs/仕様/context-stat機能仕様.md`
- `docs/手順/context-statを使う.md`
- `_worklist/`

## TODO

- [x] `mcp request`のtable出力で計測表を返却結果より先に表示する。
- [x] tree出力も同じ順序にする。
- [x] テストとドキュメントを更新する。
- [x] 検証する。

## 決定事項

- `mcp request`の人間向け出力は、`source`、計測表、結果状態、診断の順にする。`-v`/`--verbose`指定時は結果状態の後に返却結果本文を表示する。
- JSONのフィールド順や内容は変更しない。

## 未決

- なし。

## 確認手順

- `uv run pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv lock --check`
- `git diff --check`

## 結果

- tableとtreeを`source`、計測表、返却結果、診断の順序に統一した。
- `uv run pytest -q`: 81 passed
- `uv run ruff check .`: 成功
- `uv run ruff format --check .`: 成功
- `uv lock --check`: 成功
- `git diff --check`: 成功
