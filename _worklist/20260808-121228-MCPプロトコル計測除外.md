# MCPプロトコル計測除外

## 対象パス

- `src/context_stat/cli.py`
- `docs/仕様/context-stat機能仕様.md`
- `docs/設計/context-stat設計.md`
- `docs/現状/context-statの現状.md`
- `docs/外部参照/ModelContextProtocol/MCPクライアントと通信方式.md`
- `_worklist/`

## TODO

- [x] MCPのprotocol用`skip`事実をレポートから削除する。
- [x] protocolメッセージを計測対象外とする仕様・設計へ修正する。
- [x] `docs/現状/`の未完了差分からprotocol計測を削除する。
- [x] テストと差分を確認する。

## 決定事項

- MCPのinitialize、initialized、JSON-RPC envelope、通知などのprotocolメッセージは計測しない。
- 計測対象はcontext-statが扱う論理リクエスト、返却結果、contentとする。
- protocolのwire-levelトークン数を`skip`や推定値としてレポートしない。

## 未決

- なし。

## 確認手順

- `rg`で`protocol_measurement`とprotocolの`skip`が実装・現行文書に残っていないことを確認する。
- `uv run pytest -q`
- `uv run --extra tokenizers pytest -q`
- `git diff --check`

## 結果

- `_mcp_protocol_fact`とレポートの`protocol_measurement`を削除した。
- MCPのprotocolメッセージを計測対象外とし、論理payloadだけをgenerated/returnedとして計測する仕様へ修正した。
- `docs/現状/`からprotocol計測の未完了項目を削除した。
- `uv run pytest -q`: 82 passed
- `uv run --extra tokenizers pytest -q`: 82 passed
- `uv run ruff check .`: 成功
- `uv run ruff format --check .`: 成功
- `uv lock --check`: 成功
- `git diff --check`: 成功
