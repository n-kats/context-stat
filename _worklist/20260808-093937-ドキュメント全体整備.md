# ドキュメント全体整備

## 対象パス

- `README.md`
- `docs/`
- `_worklist/`

## TODO

- [x] 文書構成と目次の整合を確認する。
- [x] READMEと`docs/手順`の利用方法を整合させる。
- [x] 仕様・設計・設計判断の重複と古い記述を整理する。
- [x] `docs/現状`を未完了事項と最新の検証結果だけに保つ。
- [x] 文書間リンクと記述形式を確認する。
- [x] ドキュメント整備後の検証結果を記録する。

## 決定事項

- 実装済みの利用方法はREADMEと手順へ集約し、`docs/現状`には未完了の差分だけを残す。
- 仕様は利用者から見える挙動、設計は内部責務、設計判断は採用・不採用の理由を記載する。
- 外部参照文書は現行仕様の正の情報源にせず、根拠と参照内容だけを記録する。

## 未決

- なし。

## 確認手順

- `rg`で古いコマンド名・未整合な説明・リンクを検索する。
- Markdown内の相対リンク先を確認する。
- `git diff --check`

## 結果

- README、docsの入口、目次、仕様、手順、現状、計画を更新した。
- 手順へtable、tree、JSON、MCP返却結果、診断の出力例を追加した。
- 実装済みの項目を計画から削除し、未完了の計画だけを残した。
- docs内の相対リンクと、古いコマンド名・旧ディレクトリ名の記述を確認した。
- `uv run pytest -q`: 81 passed
- `uv run --extra tokenizers pytest -q`: 81 passed
- `uv run ruff check .`: 成功
- `uv run ruff format --check .`: 成功
- `uv lock --check`: 成功
- `git diff --check`: 成功
