# ドキュメント現行情報監査

## TODO

- [x] 現行CLIと実装にない情報を抽出する
- [x] 古い仕様・実装前の設計記述を削除または位置付ける
- [x] 外部参照ログを現行仕様と混同しない形にする
- [x] 文書構成と検証結果を確認する

## 変更予定の主要パス

- `README.md`
- `docs/仕様/`
- `docs/外部参照/`
- `docs/現状/`
- `docs/設計/`
- `docs/設計判断/`
- `_worklist/`

## 決定事項

- 現行実装に存在しない画像`auto`方式の説明は削除する。
- `初期実装時の順序`は現行設計から外す。
- 未実装のClaude・オンラインバックエンドは現行機能として読めないようにする。
- 外部参照ログは参照時点の記録であり、現行仕様ではないことを明記する。

## 未決事項

- なし

## 確認手順

- CLIヘルプとソースのオプション・既定値を照合する
- `rg`で旧仕様の語句と旧パスを検索する
- `git diff --check`を実行する
- `uv run pytest -q`を実行する

## 結果

- 画像方式の`auto`という旧説明を削除し、現行の`gpt-5.6-style`だけを記載した。
- 設計書の初期実装手順を削除し、論理境界と実装済み範囲を区別した。
- Claude・オンラインバックエンドを未実装として明記した。
- 外部参照ログを参照時点の非規範情報として明記した。
- 旧ディレクトリ・旧モデル関連の記述を検索し、現行文書に残っていないことを確認した。
- `uv run pytest -q`: 71 passed。
- `uv lock --check`: 問題なし。
- `git diff --check`: 問題なし。
