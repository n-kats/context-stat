# 作業ログ: table表示整形

## TODO

- [x] tableのメトリクス値から単位を外す
- [x] tableのメトリクス列を右寄せにする
- [x] 既存テストと表示確認を更新する

## 決定事項

- tableでは列見出しが単位を示すため、セルには値だけを表示する。
- パス、項目名、種類は左寄せ、メトリクス値は右寄せにする。
- treeの`path [XX tokens]`形式とJSONの単位情報は変更しない。

## 未決

- なし。

## 確認手順

- `uv run pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv lock --check`
- `uv tool install .`で導入したCLIのtable表示を確認する。

## 実施結果

- tableのメトリクスセルを数値だけの表示に変更し、メトリクス列を右寄せにした。
- treeは単位付きの`path [XX tokens]`形式を維持し、JSONは`unit`を含む構造を維持した。

## 変更した主要パス

- `src/context_stat/output.py`
- `tests/unit/test_cli.py`
- `tests/unit/test_output.py`
- `_worklist/20260807-230540table表示整形.md`
