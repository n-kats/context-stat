# AGENTS.md作成ログ

## TODO

- [x] `AGENTS.md` の章立てとリポジトリ固有方針を確定する。
- [x] `AGENTS.md` を作成する。
- [x] 生成物にテンプレートの `TODO:` と作成時の注意書きが残っていないことを確認する。

## 決定事項

- リポジトリの目的は、コンテキストに投入する可能性があるファイルのトークン数などを集計する `context-stat` の開発とする。
- Pythonの新しい安定版を基本とし、`uv tool install` で導入できるCLIツールを目標とする。
- `src/`、`tests/`、`docs/`、`scripts/`、`_worklist/`、`_notes/`、`_local/`、`_tmp/`、`_cache/` を用途別に扱う。
- `reports/` と `_data/` は採用しない。
- `_worklist/` と `_notes/` はコミット対象とし、`_local/`、`_tmp/`、`_cache/` はコミット対象外とする。
- 検証コマンドの参考情報として `uv run pytest`、`uv run ruff check .`、`uv run ruff format --check .`、`uv tool install .` を記載する。

## 未決事項

- なし。詳細なツール仕様は本作業の対象外とする。

## 確認手順

- テンプレート本文の章（最優先条件、プロジェクトの目的、ディレクトリ構造、禁止事項・非推奨事項、基本ワークフロー）を `AGENTS.md` に含める。
- `AGENTS.md` に `TODO:`、テンプレートの作成時注意書き、未採用の `reports/` および `_data/` の説明が残っていないことを確認する。
- `AGENTS.md` に日本語方針、禁止事項、`docs/` と `_worklist/` の運用、検証コマンドが記載されていることを確認する。
