# 作業ログ: tableとJSONの並び順統一

## TODO

- [x] tableのディレクトリ表示を`dir`へ短縮する
- [x] tableをtreeの階層順から分離し、全ノードをソートする
- [x] JSONの`nodes`をtableと同じフラットな順序・種類表示にする
- [x] テスト、仕様、READMEを更新する
- [x] pytest、ruff、インストール後のCLIで確認する

## 決定事項

- tableとJSONのディレクトリノードの`kind`は`dir`、ファイルノードは`file`とする。
- tableとJSONのpath groupは、ディレクトリ合計を含む全ノードを`--sort`と`--order`でグローバルに並べる。
- treeだけは階層を維持し、同じ階層の兄弟をソートする。
- JSONの`items`は既存のファイル単位の互換用一覧として残す。

## 未決

- なし。

## 確認手順

- `uv run pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv lock --check`
- `uv tool install .`後にtableとJSONの出力順を確認する。

## 変更した主要パス

- `src/context_stat/output.py`
- `tests/unit/test_cli.py`
- `docs/spec/context-stat機能仕様.md`
- `README.md`
- `_worklist/20260807-231138table-json並び順統一.md`

## 実施結果

- tableとJSONのpath groupを全ノードのフラットな一覧にし、`--sort`と`--order`でグローバルに並べた。
- tableとJSONのディレクトリノードを`dir`、ファイルノードを`file`として出力するようにした。
- treeは階層構造と兄弟単位のソートを維持した。
- `uv run pytest -q`は48件通過した。ruff、format、`uv lock --check`、`uv tool install`も通過した。
