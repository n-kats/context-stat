# ドキュメント構成整理

## TODO

- [x] `docs/`を日本語の用途別ディレクトリへ整理する
- [x] READMEと現行実装の対応を補強する
- [x] 仕様、設計、手順、現状、構造一覧を簡潔に整える
- [x] 文書内の旧ディレクトリ参照を確認する

## 変更予定の主要パス

- `README.md`
- `AGENTS.md`
- `docs/`
- `_worklist/`

## 決定事項

- `docs/spec`は`docs/仕様`へ移す
- `docs/design`は`docs/設計`へ移す
- `docs/web`は`docs/外部参照`へ移す
- `docs/README.md`を文書の入口とする
- `docs/directory_structure.md`を物理構成の正のソースとする
- 空の用途ディレクトリは作らず、各用途に短いREADMEを置く

## 未決事項

- 個別の手順書を機能ごとに分割する時期は、利用例が増えた時点で判断する

## 確認手順

- `rg --files docs`で構成を確認する
- 旧ディレクトリ参照が残っていないことを確認する
- `git diff --check`で文書の空白エラーを確認する

## 結果

- `docs/spec`を`docs/仕様`へ移した。
- `docs/design`を`docs/設計`へ移した。
- `docs/web`を`docs/外部参照`へ移した。
- `docs/README.md`、手順、現状、規約、計画、設計判断を追加した。
- `AGENTS.md`と`docs/directory_structure.md`を新しい構成へ更新した。
- 旧ディレクトリ参照は、作業ログなどの履歴を除いて残っていない。
- `docs/`直下が指定された11項目になっていることを確認した。
- `uv run pytest -q`: 71 passed。
- `git diff --check`: 問題なし。
