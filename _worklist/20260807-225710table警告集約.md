# table警告集約

## TODO

- [x] tableの計測値セルから推定・外部・状態注記を外す
- [x] 推定警告が標準エラーへ集約されることを回帰テストする
- [x] 仕様と作業記録を更新する
- [x] pytest、ruff、インストール確認を行う

## 決定事項

- tableは`値 単位`だけを表示し、計測状態や精度をセルへ重複表示しない。
- 精度・状態・外部送信の詳細はJSONのmetric情報に保持する。
- 推定値の警告はレポート末尾の標準エラーへ集約する。

## 変更予定パス

- `src/context_stat/output.py`
- `tests/unit/test_cli.py`
- `docs/spec/context-stat機能仕様.md`

## 確認手順

```text
uv run pytest
uv run ruff check .
uv run ruff format --check .
uv lock --check
uv tool install . --force --no-cache
```

## 実施結果

- tableの値セルから`estimated`、`local`、状態などの注記を除去し、値と単位だけを表示するようにした。
- 推定値はtreeと同じく、`measurement-estimated`として標準エラーへ集約する。
- `uv run pytest`、ruff、`uv lock --check`、`uv tool install`で確認する。
