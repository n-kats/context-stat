# 最終監査修正

## TODO

- [x] 破損画像の読み取り検証を強化し、画像トークンをskipできることを確認する
- [x] Gitの任意値オプションを正しく解釈する
- [x] 空ディレクトリもディレクトリ合計ノードとして表示する
- [x] MCPの画像contentを画像計測対象へ分配する経路を検証する
- [x] 追加テストを実行する
- [x] 全検証と実CLI確認を行う

## 決定事項

- Pillowのヘッダー読み取りだけでなく、`verify()`で画像データの破損を検証する。
- 画像を読み取れない場合は、画像属性を要求したときはfailed、画像トークンだけの要求ではskipとする。
- `--dirstat-by-file`は値を取らないGit diffオプションとして扱う。
- 空ディレクトリや配下が全て除外されたディレクトリも、空の集計nodeとして出力する。

## 未決

- なし

## 変更予定の主要パス

- `src/context_stat/adapters/image.py`
- `src/context_stat/adapters/git.py`
- `tests/unit/test_image.py`
- `tests/unit/test_adapters.py`
- `_worklist/`

## 確認手順

- `uv run pytest -q`
- `uv run --extra tokenizers pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv lock --check`
- `git diff --check`

## 実施結果

- `uv run pytest -q`: 55 passed
- `uv run --extra tokenizers pytest -q`: 55 passed
- `uv run ruff check .`: pass
- `uv run ruff format --check .`: pass
- `uv lock --check`: pass
- `git diff --check`: pass
- `uv tool install . --force`: 一時のtool/binディレクトリへ導入成功
- `context-stat --help`、tree形式のstat、ファイル単位のgit diffを実CLIで確認
