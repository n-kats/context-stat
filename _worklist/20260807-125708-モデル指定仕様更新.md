# モデル指定仕様更新ログ

## TODO

- [x] トークン計測の基準をモデル指定に変更する。
- [x] `--tokenizer` の位置づけを任意の上書き・検証用とする。
- [x] モデルと実効トークナイザーを計測結果に明記する方針を追記する。

## 決定事項

- トークン計測の主な指定方法は `--model` とする。
- モデルから対応するトークナイザーを決定する。
- `--tokenizer` は必要な場合だけ明示する任意の指定とする。
- モデルとトークナイザーの組み合わせは計測条件として出力する。

## 未決事項

- モデルの既定値と対応範囲
- モデルからトークナイザーを決定する具体的な実装方法

## 確認手順

- 仕様書に `--model` が主指定であることが記載されていることを確認する。
- `--tokenizer` が任意の上書き・検証用として記載されていることを確認する。
- 計測結果にモデルと実効トークナイザーを明記する方針があることを確認する。
