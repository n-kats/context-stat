# 再レビュー指摘修正

## TODO

- [x] シンボリックリンクをpath集計へ含める
- [x] patchを生成しないGit出力モードを拒否する
- [x] コマンド対象の画像判定を内容ベースに統一する
- [x] 小さい出力上限でも切り詰め状態を診断する
- [x] 画像読み取りエラーの文言を安定化する
- [x] 画像メトリクスの失敗順を固定する
- [x] 回帰テストを追加する
- [x] 全検証を完了する

## 決定事項

- pathの表示・集計ではシンボリックリンクを解決せず、入力時の字句パスを使う。
- Git差分はpatch本文を生成するモードだけを受け付ける。
- コマンド実行時の画像除外は、拡張子ではなく画像内容の判定も使う。
- 出力切り詰めの理由は、出力本文とは別に実行結果メタデータと診断へ記録する。
- 画像読み取りエラーは固定メッセージにし、画像メトリクスはwidth、height、frames、image_tokensの順にする。

## 未決

- なし

## 変更予定の主要パス

- `src/context_stat/output.py`
- `src/context_stat/adapters/git.py`
- `src/context_stat/adapters/process.py`
- `src/context_stat/adapters/image.py`
- `src/context_stat/domain/service.py`
- `src/context_stat/cli.py`
- `docs/spec/`
- `docs/design/`
- `tests/unit/`
- `_worklist/`

## 確認手順

- `uv run pytest -q`
- `uv run --extra tokenizers pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv lock --check`
- `git diff --check`
- `uv tool install .`

## 結果

- 通常依存のテスト71件、`tokenizers`追加依存のテスト71件が成功した。
- ruff、format、lock、差分検査、`uv tool install .`が成功した。
