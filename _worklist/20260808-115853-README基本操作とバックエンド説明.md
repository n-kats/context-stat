# README基本操作とバックエンド説明

## 対象パス

- `README.md`
- `_worklist/`

## TODO

- [x] READMEにバックエンドの選択、導入、外部送信条件を説明する。
- [x] 基本操作ごとの用途と結果例を記載する。
- [x] 既存の仕様・手順との不一致がないか確認する。
- [x] 文書の差分を検証する。

## 決定事項

- READMEは最初に全体像を確認するための文書とし、詳細な制約は既存の仕様・手順へ委ねる。
- 結果例は代表的な出力の形を示し、実行環境で変わるトークン数などは固定値として扱わない。
- 現行バックエンドは`tiktoken`と`tokenizers`として説明する。

## 未決

- なし。

## 確認手順

- `git diff --check`
- READMEのバックエンド説明と各操作の例を目視確認する。

## 結果

- `auto`、`tiktoken`、`tokenizers`の違い、導入方法、トークナイザー指定、外部送信条件をREADMEへ追加した。
- `stat`、`jinja`、`git diff`、`mcp list`、`mcp request`、`completions`の用途と代表的な結果例をREADMEへ追加した。
- `-v`によるMCP返却本文の表示条件と、画像結果の要約表示も操作例へ反映した。
- `git diff --check`: 成功
