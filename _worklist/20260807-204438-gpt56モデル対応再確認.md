# GPT-5.6モデル対応再確認

## TODO

- [x] GPT-5.6系4モデルの公式モデルIDとtiktoken登録状態を確認する。
- [x] `o200k_base`推定を`estimated`として実装し、直接登録時は`exact`を優先する。
- [x] 初期実装の画像、Git、Jinja、MCP、CLIの未完了箇所を仕様と照合する。
- [x] テスト、Ruff、uv、インストール後CLIを再検証する。

## 決定事項

- 対象モデルは`gpt-5.6`、`gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`に限定する。
- GPT-5.5以前は今回の対象に追加しない。
- 存在しないモデルIDを推定対象として追加しない。
- tiktokenがGPT-5.6系を直接登録していない間は、`o200k_base`による計測を`estimated`とする。

## 参照・確認手順

- `docs/web/OpenAI/tiktokenとGPT-5.6対応.md`
- `uv run pytest`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv lock --check`
- `uv tool install .`相当の導入後CLI実行

## 変更予定パス

- `src/context_stat/adapters/token.py`
- `tests/unit/test_token.py`
- `docs/spec/context-stat機能仕様.md`
- `docs/web/OpenAI/tiktokenとGPT-5.6対応.md`
- `_worklist/`

## 実施結果

- GPT-5.6系4モデルを`tiktoken 0.13.0`で確認し、直接登録がないことを確認した。
- 4モデルの`o200k_base`選択を`estimated`として扱うテストを追加した。
- GPT-5.6系の画像トークンは、公開資料からローカルで再現可能な式を確定できないため`unsupported`とした。
- Claude 5の正式IDに対応する画像ポリシー、Git差分パーサー、`.gitignore`反映、非画像バイナリ除外、対象付き警告表示を実装した。
- `uv run pytest`は29件、`uv run ruff check .`、`uv run ruff format --check .`、`uv lock --check`を通過した。
