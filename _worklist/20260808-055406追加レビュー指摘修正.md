# 追加レビュー指摘修正

## TODO

- [x] Streamable HTTP fixtureを変更へ含める方法を確定する
- [x] 再帰走査の画像検出を修正する
- [x] 不明なメトリクス名を拒否する
- [x] 壊れたtokenizer.jsonをCLIエラーへ変換する
- [x] コマンド起動失敗を対象単位で継続処理する
- [x] Git未導入時のGitignore判定をスキップする
- [x] 指摘ごとの回帰テストを追加する
- [x] 全検証を行う

## 決定事項

- 画像拡張子に依存せず、再帰走査でも画像形式を判定する。
- `all`を指定した場合も、同時に指定された全メトリクス名を検証する。
- 外部ライブラリの読み込み・ファイル解析失敗は、利用者が読める例外へ変換する。
- コマンド起動失敗は対象ごとの失敗結果としてレポートし、他の対象の処理を継続する。
- Gitが利用できない場合は、Gitignore判定を行わず通常のディレクトリとして扱う。
- Streamable HTTPの実機fixtureは、追跡済みの`mcp_stdio_server.py`を環境変数でHTTP起動する方式に統合した。

## 未決

- なし

## 変更予定の主要パス

- `src/context_stat/adapters/filesystem.py`
- `src/context_stat/adapters/token.py`
- `src/context_stat/adapters/process.py`
- `src/context_stat/domain/measurement.py`
- `src/context_stat/cli.py`
- `tests/fixtures/`
- `tests/unit/`
- `_worklist/`

## 確認手順

- `uv run pytest -q`
- `uv run --extra tokenizers pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv lock --check`
- `git diff --check`
- `uv tool install .`

## 結果

- 通常依存のテスト66件、`tokenizers`追加依存のテスト66件が成功した。
- ruff、format、lock、差分検査、`uv tool install .`が成功した。
